# voicefox 键盘快捷键参考

## 全局快捷键

以下快捷键是全局绑定；其中快进/快退会按页面限定，搜索页的 `[` / `]` 由音源切换使用：

### 导航

| 按键 | 功能 |
|------|------|
| `1` | 切换到**队列**页面 |
| `2` | 切换到**搜索**页面 |
| `3` | 切换到**排行榜**页面 |
| `4` | 切换到**热门歌单**页面 |
| `5` | 切换到**收藏歌曲**页面 |
| `6` | 切换到**历史**页面 |
| `7` | 切换到**本地音乐**页面 |
| `8` | 切换到**设置**页面 |
| `/` | 切换到搜索页面 |
| `Tab` | 切换到下一个标签页 |
| `Shift+Tab` | 切换到上一个标签页 |
| `\` | 打开**快捷键说明浮层**（展示当前配置生效的全部键位，`j`/`k` 或方向键滚动，`Ctrl+d`/`Ctrl+u` 翻页，`Esc` 关闭） |

### 播放控制

| 按键 | 功能 |
|------|------|
| `Space` | 播放 / 暂停 |
| `n` 或 `>` | 下一首 |
| `b` 或 `<` | 上一首 |
| `m` | 依次切换播放模式：列表循环 → 单曲循环 → 随机 → 顺序播放 → 播完停止 |
| `]` | 队列页面快进 5 秒；搜索页面切换到下一个音源 |
| `[` | 队列页面后退 5 秒；搜索页面切换到上一个音源 |

### 音量控制

| 按键 | 功能 |
|------|------|
| `.` | 音量增加 5% |
| `,` | 音量减少 5% |

通过快捷键或 MPRIS 修改后的音量会自动保存到配置文件，并在下次启动时恢复。

### 界面

| 按键 | 功能 |
|------|------|
| `Ctrl+R` | 强制重绘，包括封面图片（若有） |

### 歌曲管理

| 按键 | 功能 |
|------|------|
| `Ctrl+L` | 收藏 / 取消收藏当前播放中的歌曲（所有页面一致） |
| `Ctrl+S` | 下载当前播放中的歌曲（队列页改为下载选中的队列项） |
| `Ctrl+O` | 打开 / 关闭下载面板（`c` 取消或移除选中任务、`x` 清理已结束任务、`C` 清空下载历史、`Esc` 关闭） |

歌曲或歌单列表中的 `f` 用于收藏 / 取消收藏当前选中的歌曲或歌单。

在搜索、排行榜歌曲、热门歌单歌曲、收藏、历史和本地音乐列表中，选中歌曲后：

| 按键 | 功能 |
|------|------|
| `a` | 将歌曲追加到播放队列末尾，不替换当前队列 |
| `Shift+A` | 将歌曲插入当前播放歌曲之后，作为下一首 |
| `D` | 下载选中的歌曲（本地音乐页不提供，本地文件已在磁盘上） |

如果当前队列为空，`Shift+A` 会将歌曲作为队列中的第一首；加入队列不会自动开始播放。

下载同样可以从歌曲右键菜单的「下载歌曲」进入；队列页的 `D` 已用于清空队列，下载请用 `Ctrl+S`。
下载进度、失败原因与取消操作都在下载面板（`Ctrl+O`）里查看。

下载完成后会在数据目录留下一条历史记录（`~/.config/voicefox/data/downloads.json`），面板底部显示最近 5 条。记录同时充当去重索引：同一首歌（按「歌手 - 歌名」）即使换过文件名模板、或重启过程序，也不会被重复下载；手动删掉本地文件后，这首歌会重新变为可下载。面板里按 `C` 可以清空历史与去重索引。

### WebDAV 同步

设置页的「下载」分类里：

| 按键 | 功能 |
|------|------|
| `Z` | 打开 / 关闭下载后同步到 WebDAV |
| `C` | 编辑 WebDAV 地址（可直接粘贴 `https://用户:密码@主机/路径`，保存时会拆分账号密码） |

远端目录、账号密码的其它字段见 `config.toml` 的 `[download.webdav]`。同步失败只发警告，本地文件不受影响。

### 扫码登录

设置页「集成」分类里的「扫码登录」行（`b`）打开音源选择浮层，列出当前支持扫码登录的音源（哔哩哔哩、网易云音乐、酷狗音乐、QQ 音乐）及其状态：

| 按键 | 功能 |
|------|------|
| `b` | 打开扫码登录选择器 |
| `j` / `k` | 在选择器里移动 |
| `Enter` | 未登录则打开二维码登录；已登录则退出登录 |
| `x` | 直接退出选中音源的登录 |
| `Esc` | 关闭选择器 |

登录页里按 `Esc` / `q` 关闭。登录态保存在 `~/.config/voicefox/session/<音源>.json`（权限 0600），登录后网易云、酷狗、QQ 音乐的 VIP / 无损请求会自动带上 cookie。QQ 的二维码是服务端生成的图片，登录页会把图片渲染成半块字符显示，终端过窄时会提示调整尺寸。

我的歌单：网易云与酷狗登录后，歌单页按 `m` 可以浏览账号下的歌单（QQ 的个人歌单接口还需要额外的登录后校验，暂未接入）。

### 其他

| 按键 | 功能 |
|------|------|
| `q` | 退出 voicefox |
| `Esc` | 取消当前操作；在有内层列表时返回上一层，空闲时停留在当前标签页 |

### Windows 退出行为

- 按 `q` 会停止播放、清理终端界面并退出。
- 播放核心通过 libmpv 嵌入 voicefox 进程；直接关闭终端窗口时不会留下独立播放器进程。

---

## 队列页面（Main）

主页面显示当前播放队列、歌曲信息和歌词。

### 队列操作

| 按键 | 功能 |
|------|------|
| `k` | 选择上一首队列歌曲 |
| `j` | 选择下一首队列歌曲 |
| `Home` 或 `g` | 跳到队列顶部 |
| `End` 或 `G` 或 `Shift+G` | 跳到队列底部 |
| `PageUp` 或 `Ctrl+U` | 向上翻半页 |
| `PageDown` 或 `Ctrl+D` | 向下翻半页 |
| `Enter` 或 `l` | 播放选中的队列歌曲 |
| `Shift+↑` 或 `Shift+K` | 将选中歌曲向前移动一位 |
| `Shift+↓` 或 `Shift+J` | 将选中歌曲向后移动一位 |
| `d` 或 `Delete` | 从队列移除选中的一首歌曲 |
| `Shift+D` | 清空整个播放队列 |

也可以按住鼠标左键拖动队列歌曲来调整顺序。

### 播放控制

| 按键 | 功能 |
|------|------|
| `←` | 后退 5 秒 |
| `→` | 快进 5 秒 |
| `↑` | 音量增加 5% |
| `↓` | 音量减少 5% |

> 注意：`←`/`→`/`↑`/`↓` 仅在队列页面中映射为播放控制。
> 在其他页面中方向键用于列表导航。

---

## 搜索页面（Search）

### 输入模式

按 `i` 或 `/` 进入搜索输入模式（如果已处于输入模式，直接输入文字）。

| 按键 | 功能 |
|------|------|
| 输入文字 | 自动触发搜索（300ms 防抖） |
| `Backspace` | 删除上一个字符 |
| `Enter` | 当输入框非空时手动触发搜索 |
| `Esc` | 退出输入模式 |

输入框里粘贴歌曲 / 歌单 / 专辑的分享链接（`http://` 或 `https://`）时，会直接解析该链接而不是把整条 URL 当关键词搜索：单曲链接替换结果列表为这一首，歌单 / 专辑链接展开成曲目列表，并提示「已解析歌单「…」共 N 首」。目前支持网易云（单曲/歌单/专辑）、咪咕（单曲/歌单/专辑）、千千音乐（单曲/歌单/专辑）、JOOX（单曲/歌单/专辑）、Jamendo（单曲/歌单/专辑）、Apple Music（单曲/歌单/专辑）、汽水音乐（单曲/歌单）、哔哩哔哩（视频）、酷狗（单曲/歌单）、酷我（单曲/歌单）、QQ 音乐（单曲/歌单）、5sing（单曲/歌单）的链接，其它平台会提示改用关键词搜索。

### 结果列表导航

| 按键 | 功能 |
|------|------|
| `↑` 或 `k` | 选择上一首歌曲 |
| `↓` 或 `j` | 选择下一首歌曲 |
| `Home` 或 `g` | 跳到列表顶部 |
| `End` 或 `G` 或 `Shift+G` | 跳到列表底部 |
| `PageUp` 或 `Ctrl+U` | 向上翻半页 |
| `PageDown` 或 `Ctrl+D` | 向下翻半页 |
| `Enter` 或 `l` | 播放当前选中的歌曲 |
| `a` / `A` | 加入队尾 / 插到当前歌曲之后 |

### 音源控制

搜索范围只显示当前已启用的在线音源；禁用音源不会出现在顶部标签中。

| 按键 | 功能 |
|------|------|
| `v` | 打开当前歌曲的其他音源版本列表 |
| `←` 或 `[` | 切换到上一个搜索范围或音源 |
| `→` 或 `]` | 切换到下一个搜索范围或音源 |
| `Esc` | 退出输入或弹层；空闲时停留在搜索页面 |

选中歌曲后，`@` 追搜歌手（哔哩哔哩结果自动限定为 UP 主），`#` 追搜专辑；再次按相同按键可删除该条件并恢复基础关键词。

### 歌手 / 专辑详情

在任意歌曲列表右键选择「查看歌手」或「查看专辑」即可打开详情浮层：

| 按键 | 功能 |
|------|------|
| `↑` / `↓` 或 `k` / `j` | 在歌曲列表中选择 |
| `Tab` 或 `←` / `→` | 歌手页在「专辑」和「歌曲」两个区域间切换 |
| `Enter` | 播放选中歌曲；歌手页选中专辑时进入专辑详情 |
| `p` | 播放全部 |
| `a` / `A` | 加入队列尾部 / 下一首 |
| `f` | 收藏选中歌曲 |
| `g` / `G` | 跳到顶部 / 底部 |
| `Ctrl+u` / `Ctrl+d` | 翻页 |
| `Esc` | 关闭详情（专辑详情返回歌手页所在层级） |

歌手页的歌曲列表支持自动分页：滚动接近列表末尾时会自动追加下一页，专辑列表随分页去重合并。

打开音源版本列表后：

| 按键 | 功能 |
|------|------|
| `↑` / `↓` 或 `k` / `j` | 选择其他音源版本 |
| `Enter` | 使用选中的音源版本播放 |
| `a` / `A` | 将选中的音源版本加入队尾 / 插到当前歌曲之后 |
| `Esc` 或 `v` | 关闭音源版本列表 |

打开哔哩哔哩多分 P 视频后：

| 按键 | 功能 |
|------|------|
| `↑` / `↓` 或 `k` / `j` | 选择分 P |
| `Home` / `End` | 跳到首个 / 最后一个分 P |
| `Enter` 或 `l` | 从选中的分 P 开始播放 |
| `a` / `A` | 将选中的分 P 加入队尾 / 插到当前歌曲之后 |
| `Esc` | 取消选择，不开始播放 |

### 鼠标操作

- 点击歌曲行 → 选中该歌曲
- 双击歌曲行 → 播放该歌曲
- 右键歌曲行 → 打开歌曲操作菜单
- 滚轮 → 上下滚动列表

---

## 排行榜页面（Leaderboard）

### 榜单列表

首次进入时显示可用的排行榜列表。

| 按键 | 功能 |
|------|------|
| `↑` 或 `k` | 选择上一个榜单 |
| `↓` 或 `j` | 选择下一个榜单 |
| `Enter` 或 `→` | 进入选中的榜单，查看歌曲列表 |
| `Home` 或 `g` | 跳到顶部 |
| `End` 或 `G` 或 `Shift+G` | 跳到底部 |
| `PageUp` 或 `Ctrl+U` | 向上翻半页 |
| `PageDown` 或 `Ctrl+D` | 向下翻半页 |

### 歌曲列表

进入榜单后显示该榜单的歌曲。

| 按键 | 功能 |
|------|------|
| `↑` 或 `k` | 选择上一首歌曲 |
| `↓` 或 `j` | 选择下一首歌曲 |
| `Enter` 或 `l` | 播放选中的歌曲（保持在榜单歌曲页） |
| `a` / `A` | 加入队尾 / 插到当前歌曲之后 |
| `←` 或 `Esc` | 返回榜单列表 |
| `Home` 或 `g` | 跳到顶部 |
| `End` 或 `G` 或 `Shift+G` | 跳到底部 |
| `PageUp` 或 `Ctrl+U` | 向上翻半页 |
| `PageDown` 或 `Ctrl+D` | 向下翻半页 |

### 鼠标操作

- 点击榜单行 → 进入该榜单
- 点击歌曲行 → 选中
- 双击歌曲行 → 播放
- 右键歌曲行 → 打开歌曲操作菜单

---

## 热门歌单页面（Playlists）

顶部可在**已收藏、酷我、酷狗、QQ、网易云、咪咕**之间切换。在线音源的热门歌单会分别从对应音源加载。

| 按键 | 功能 |
|------|------|
| `←` / `→` 或 `[` / `]` | 切换上一个 / 下一个音源 |
| `↑` 或 `k` | 选择上一个歌单或歌曲 |
| `↓` 或 `j` | 选择下一个歌单或歌曲 |
| `Enter` 或 `l` | 进入歌单；在歌曲列表中播放选中歌曲 |
| `a` / `A` | 在歌曲列表中加入队尾 / 插到当前歌曲之后 |
| `←` / `h` / `Esc` | 从歌曲列表返回歌单列表 |
| `f` | 收藏 / 取消收藏当前选中的歌单；在歌曲列表中收藏选中歌曲 |
| `r` | 刷新当前音源的热门歌单或当前歌单详情 |

在音源歌单列表按 `/` 或 `s` 输入关键词并回车可搜索歌单；`Esc` 取消输入，`c` 清除搜索并恢复热门歌单。音源不支持搜索时会自动回退到热门歌单。

歌单分类：在支持分类的音源（网易云、咪咕、千千音乐、酷狗、酷我、QQ 音乐）按 `c` 打开分类选择浮层，`j` / `k` 移动、`Enter` 应用、`Esc` 取消。选择后标题会显示当前分类（如「分类 华语」），列表按该分类重新加载；「全部」恢复默认分类。

| 按键 | 功能 |
|------|------|
| `c` | 打开歌单分类选择器（仅在音源歌单列表、且没有在搜索时；自建歌单页的 `c` 是新建歌单） |
| `j` / `k` / `↑` / `↓` | 在选择器里移动 |
| `g` / `G` | 跳到选择器首项 / 末项 |
| `Enter` | 应用选中的分类 |
| `Esc` | 关闭选择器 |

我的歌单：在已登录的音源按 `m` 在「热门歌单」与「我的歌单」之间切换。未登录时会提示先去设置页扫码；拉取失败（多为登录失效）会自动退回热门歌单并说明原因。

在线热门歌单支持自动分页：选中并滚动到当前列表末尾后，程序会加载下一页；已收藏歌单和歌单歌曲详情不触发分页。

### 鼠标操作

- 点击顶部音源 → 切换音源
- 双击歌单行 → 进入歌单
- 双击歌曲行 → 播放歌曲
- 右键歌曲行 → 打开歌曲操作菜单
- 右键歌单行 → 收藏 / 取消收藏

---

## 本地歌词与封面

- 本地歌曲优先读取与音频文件同名的 `.lrc` 文件。
- 没有 `.lrc` 文件时，会尝试读取音频内嵌歌词。
- 支持带时间戳的内嵌 LRC；纯文本内嵌歌词也会显示，并按歌曲时长分配显示时间。
- 封面只在队列主页显示，切换到本地音乐、历史等页面时会清理终端图片图层。

---

## 收藏页面（Favorites）

显示所有收藏的歌曲。

| 按键 | 功能 |
|------|------|
| `↑` 或 `k` | 选择上一首 |
| `↓` 或 `j` | 选择下一首 |
| `Home` 或 `g` | 跳到顶部 |
| `End` 或 `G` 或 `Shift+G` | 跳到底部 |
| `PageUp` 或 `Ctrl+U` | 向上翻半页 |
| `PageDown` 或 `Ctrl+D` | 向下翻半页 |
| `Enter` 或 `l` | 播放选中的歌曲（保持在收藏页面） |
| `a` / `A` | 加入队尾 / 插到当前歌曲之后 |
| `s` | 循环切换排序方式 |
| `/` | 进入筛选模式，输入文字过滤收藏歌曲 |
| `Backspace` | 删除筛选输入（筛选模式下） |
| `Esc` | 退出筛选模式；空闲时停留在收藏页面 |
| `d` / `Delete` | 取消收藏选中的歌曲 |
| `f` | 收藏 / 取消收藏选中的歌曲 |

---

## 历史页面（History）

显示所有播放过的歌曲记录。

| 按键 | 功能 |
|------|------|
| `↑` 或 `k` | 选择上一首 |
| `↓` 或 `j` | 选择下一首 |
| `Home` 或 `g` | 跳到顶部 |
| `End` 或 `G` 或 `Shift+G` | 跳到底部 |
| `PageUp` 或 `Ctrl+U` | 向上翻半页 |
| `PageDown` 或 `Ctrl+D` | 向下翻半页 |
| `Enter` 或 `l` | 播放选中的歌曲（保持在历史页面） |
| `a` / `A` | 加入队尾 / 插到当前歌曲之后 |
| `s` | 循环切换排序方式 |
| `/` | 进入筛选模式，输入文字过滤播放历史 |
| `Backspace` | 删除筛选输入（筛选模式下） |
| `Esc` | 退出筛选模式 |
| `d` / `Delete` | 删除选中的一条历史 |
| `f` | 收藏 / 取消收藏选中的歌曲 |
| `Shift+D` | 清空全部播放历史 |

---

## 本地音乐页面（LocalMusic）

显示本地音乐库中的歌曲列表。

| 按键 | 功能 |
|------|------|
| `↑` 或 `k` | 选择上一首 |
| `↓` 或 `j` | 选择下一首 |
| `Enter` 或 `l` | 播放选中的歌曲 |
| `a` / `A` | 加入队尾 / 插到当前歌曲之后 |
| `s` | 循环切换排序方式 |
| `/` | 进入筛选模式，输入文字过滤本地音乐 |
| `Backspace` | 删除筛选输入（筛选模式下） |
| `Esc` | 退出筛选模式 |
| `Home` 或 `g` | 跳到列表顶部 |
| `End` 或 `G` 或 `Shift+G` | 跳到列表底部 |
| `PageUp` 或 `Ctrl+U` | 向上翻半页 |
| `PageDown` 或 `Ctrl+D` | 向下翻半页 |
| `r` | 重新扫描所有音乐目录 |
| `d` 或 `Delete` | 打开确认框，删除选中歌曲对应的本地文件 |
| `f` | 收藏 / 取消收藏选中的歌曲 |

鼠标操作：滚轮移动选择，左键单击选中歌曲，左键双击播放歌曲。

收藏、历史和本地音乐页面会在底部状态栏显示当前排序方式。排序顺序依次为：最新/最早、名称升序/降序、歌手升序、专辑升序、时长升序/降序；最后一项在收藏和历史中为来源升序，在本地音乐中为路径升序。本地音乐的最新/最早按文件修改时间计算。

删除确认框打开后，按 `y` 确认永久删除文件；按 `n` 或 `Esc` 取消。确认框打开期间，其余键盘和鼠标操作不会穿透到页面或全局快捷键。

---

## 设置页面（Settings）

设置页包含播放、音源、歌词、网络、界面、通知、集成、本地音乐和状态栏配置。大多数设置项可直接点击；JS 音源、本地目录和状态栏内容列表使用 `s` 循环切换焦点区域。

### 设置选项

| 按键 / 操作 | 功能 |
|------|------|
| `t` | 开启 / 关闭鼠标控制 |
| `g` | 开启 / 关闭聚合搜索 |
| `w` | 开启 / 关闭列表循环导航 |
| `c` | 开启 / 关闭封面显示 |
| `e` | 开启 / 关闭播放队列、当前曲目、进度和播放状态恢复 |
| 点击“播放音质” | 依次切换 128k / 320k / FLAC / Hi-Res |
| `m` | 切换播放模式 |
| `Shift+H` | 切换历史记录保留上限 |
| 编辑 `[source].default` | 修改默认在线音源 |
| `u` | 开启 / 关闭播放失败时自动换源 |
| 编辑 `[source].enabled` | 修改启用的在线音源组合 |
| `Shift+T` | 开启 / 关闭翻译歌词 |
| `Shift+Y` | 开启 / 关闭逐字歌词 |
| 点击“歌词偏移” | 每次增加 100 ms；反向调整可编辑配置文件 |
| 点击“网络代理” | 输入代理地址，留空表示关闭 |
| `Shift+N` | 切换网络超时 |
| `Shift+P` | 切换封面协议，下次启动生效 |
| `f` | 切换最大 FPS，下次启动生效 |
| `z` | 切换列表滚动步长 |
| `i` | 开启 / 关闭 MPRIS，下次启动生效 |
| `o` / `Shift+O` | 切换 TUI 通知 / 通知停留时间 |
| `x` / `Shift+X` | 切换桌面通知 / 通知封面 |
| `Shift+R` | 开启 / 关闭切歌桌面通知 |
| `p` | 切换主题强调色 |
| `Shift+D` | 切换本地音乐扫描深度 |
| `b` | 哔哩哔哩扫码登录 / 退出 |

### 下载选项

设置页的「下载」分类（`←` / `→` 切换分类）集中管理下载参数：

| 按键 | 功能 |
|------|------|
| `Shift+S` | 编辑下载目录（支持 `~/`，留空表示 `~/Music/voicefox`） |
| `Shift+F` | 切换下载音质：跟随播放 → 128K → 320K → FLAC → Hi-Res |
| `Shift+M` | 编辑文件名模板，支持 `{name}` `{singer}` `{album}` `{source}` `{quality}` |
| `Shift+B` | 开启 / 关闭多线程分片下载 |
| `Shift+V` | 切换分片阈值：1 / 2 / 5 / 10 / 20 / 50 MB |
| `Shift+W` | 切换分片并发数：1 / 2 / 4 / 8 / 16 |
| `Shift+A` | 切换同时下载的歌曲数：1 / 2 / 3 / 4 |
| `Shift+E` | 切换失败重试次数：0 / 1 / 2 / 3 / 5 |
| `Shift+U` | 开启 / 关闭落盘字节数校验 |
| `Shift+L` | 开启 / 关闭跳过已存在的文件 |
| `Shift+J` | 开启 / 关闭写入标签 |
| `Shift+G` | 开启 / 关闭嵌入封面 |
| `Shift+I` | 开启 / 关闭保存歌词（`.lrc` + 内嵌） |

> 设置页的播放与数据操作默认使用功能键，数字键 `1` 到 `8` 始终保留给标签页。播放音质、代理和歌词偏移也可以直接用鼠标点击。

> 设置页的选项键覆盖了大半个字母表，因此这些选项键会优先由设置页处理；数字标签、`Tab` 和全局播放控制仍按全局规则工作。裸 `Esc` 在顶层不会把页面送回队列。

### JS 音源管理（焦点在 JS 音源区域）

| 按键 | 功能 |
|------|------|
| `a` | 添加 JS 音源 URL |
| `d` | 删除选中的 JS 音源 |
| `↑` 或 `k` | 选择上一个音源 |
| `↓` 或 `j` | 选择下一个音源 |
| `s` | 切换到本地音乐目录区域 |

可以配置多个 JS 音源。启动时会按配置顺序加载全部可用脚本，并依次用于播放地址、歌词、封面解析和聚合搜索。通过设置页添加的新 URL 会插入列表最前面，因此最后添加的脚本最先尝试；需要固定顺序时可直接编辑 `config.toml` 中的 `js_sources`。

### 播放失败回退顺序

开启自动换源后，在线歌曲按以下顺序处理：

1. 从当前歌曲尚未尝试的 JS 音源继续解析。
2. 尝试歌曲对应平台的内置音源。
3. 在其他已启用平台中匹配同一首歌，并重新按 JS 音源和内置音源顺序解析。
4. 所有方式都失败后自动播放队列下一首。

如果某个 JS 脚本返回了 URL，但 libmpv 实际无法播放，程序会记录该脚本并从下一个脚本继续。单曲循环不会反复重试同一首失败歌曲；列表循环在整队列都失败后停止。

### 本地音乐目录管理（焦点在本地音乐目录区域）

| 按键 | 功能 |
|------|------|
| `a` | 添加本地音乐目录路径 |
| `d` | 删除选中的目录 |
| `r` | 重新扫描所有目录 |
| `↑` 或 `k` | 选择上一个目录 |
| `↓` 或 `j` | 选择下一个目录 |
| `s` | 切换到状态栏内容区域 |

### 状态栏内容管理（焦点在状态栏内容区域）

| 按键 / 操作 | 功能 |
|------|------|
| `↑` 或 `k` | 选择上一个字段 |
| `↓` 或 `j` | 选择下一个字段 |
| `Space` / `Enter` | 显示或隐藏选中字段 |
| `Shift+↑` / `Shift+←` | 将启用的字段向左移动一位 |
| `Shift+↓` / `Shift+→` | 将启用的字段向右移动一位 |
| `s` | 切换到 JS 音源区域 |
| 点击字段复选框 | 显示或隐藏该字段 |

列表中的序号表示字段在底部状态栏中的位置。配置会立即写入 `config.toml` 的 `[ui].status_bar_items`；数组顺序就是从左到右的显示顺序，未列出的字段不会显示。窄终端中会优先保留数组前面的内容。

扫码登录会在不透明弹窗中显示二维码；终端空间不足时会提示调整窗口尺寸。

在 JS 音源 URL、本地目录或代理输入框中，按 `Enter` 保存，按 `Esc` 取消。

---

## 鼠标操作

如果配置文件中 `enable_mouse = true`（默认开启），以下鼠标操作可用：

| 操作 | 功能 |
|------|------|
| 点击标签栏 | 切换页面 |
| 点击进度条 | 按实际进度条位置跳转到指定位置 |
| 双击内容区歌曲行 | 播放该歌曲 |
| 滚轮 | 上下滚动列表 |
| 点击设置项 | 选中 / 执行 |
| 右键歌曲行 | 打开歌曲操作菜单 |
| 点击浮动通知 | 立即关闭当前通知 |

---

## 歌曲右键菜单

右键菜单适用于队列、搜索、排行榜歌曲、歌单歌曲、收藏、历史和本地音乐。

| 菜单项 | 功能 |
|------|------|
| 播放 | 使用当前列表播放选中歌曲 |
| 设为下一首 | 插入当前播放歌曲之后 |
| 加入队尾 | 追加到当前队列末尾 |
| 收藏歌曲 / 取消收藏 | 切换歌曲收藏状态 |
| 排序：当前方式（切换） | 仅在收藏、历史和本地音乐页面显示，切换到下一种排序 |
| 从队列移除 | 仅在队列页面显示 |
| 删除这条历史 | 仅在历史页面显示，删除当前记录 |
| 清空播放历史 | 仅在历史页面显示，清空全部记录 |
| 删除本地文件 | 仅在本地音乐页面显示，仍需二次确认 |

菜单支持方向键、当前页面配置的列表上下键、`Enter` 和当前页面的激活键。按 `Esc`、`q` 或再次右键可关闭，菜单打开期间其他全局快捷键不会穿透。

---

## 常见操作流程

### 搜索并播放歌曲

```
按 / 或 2    → 进入搜索页面
输入关键词     → 自动搜索
按 j/k 选择   → 选中歌曲
按 Enter     → 播放
```

### 收藏当前歌曲

```
在任何页面 → 按 Ctrl+L
收藏成功后会显示通知
```

### 切换播放模式

```
按 m → 依次切换：
  列表循环（默认） → 单曲循环 → 随机播放 → 顺序播放 → 播完停止
```

### 调整和删除队列歌曲

```
按 1                  → 进入队列页面
按 j / k              → 选择队列歌曲
按 Shift+J / Shift+K  → 将歌曲向后 / 向前移动
按 d                  → 从队列移除选中的一首歌曲
按 Shift+D            → 清空整个队列
```

### 删除本地音乐文件

```
按 7            → 进入本地音乐页面
按 j / k        → 选择歌曲
按 d / Delete   → 打开文件删除确认框
按 y            → 永久删除文件
按 n / Esc      → 取消删除
```

### 切换搜索音源

```
在搜索页面按 ← / → → 切换搜索范围或音源
选中歌曲后按 v      → 查看该歌曲的其他音源版本
在版本列表中按 Enter → 使用选中的音源播放
```

### 查看和管理收藏

```
按 5            → 进入收藏页
按 /            → 输入关键字筛选
按 d            → 取消收藏选中歌曲
```

### 浏览和收藏热门歌单

```
按 4            → 进入热门歌单页
按 ← / →        → 切换音源
按 Enter        → 查看歌单歌曲
按 f            → 收藏或取消收藏歌单
滚动到列表末尾    → 自动加载下一页
```

### 自定义 JS 音源

```
按 8            → 进入设置页
保持焦点在 JS 音源区域，按 a
输入 JS 音源 URL → 按 Enter 确认
可重复添加多个 URL；新添加项位于最前，并优先参与解析与搜索
```
