# voicefox 0.3.15 用户与功能说明

voicefox 是 Rust + ratatui + libmpv 构建的终端音乐播放器。当前 `voicefox-old` 发布线保留原有稳定 TUI 页面，同时整合新的音源、下载、登录、歌词、封面、本地音乐和跨平台运行时能力。

## 页面

1. **队列**：播放队列、歌曲信息、歌词、拖拽排序、筛选、全屏歌词。
2. **搜索**：聚合搜索、音源切换、`@` 歌手追搜、`#` 专辑追搜、音源版本选择。
3. **排行榜**：按音源浏览实时榜单。
4. **热门歌单**：按音源浏览、搜索、分页和收藏歌单。
5. **收藏**：歌曲收藏、筛选、排序。
6. **历史**：播放历史、筛选、排序、删除。
7. **本地音乐**：目录监听、标签、封面、歌词、CUE 分轨、文件诊断。
8. **设置**：音源、播放器、界面、歌词、下载和快捷键等配置。

完整快捷键见 [`../KEYBINDINGS.md`](../KEYBINDINGS.md)。

> 0.3.15 快捷键重点：`↑/↓` 可用于列表导航；队列 `/` 进入筛选，`V` 切换全屏歌词；搜索 `@` 追搜歌手、`#` 追搜专辑；下载面板支持 `↑/↓`、`Del` 与鼠标右键。全局 `Ctrl+↑/↓` 调整音量。

## 播放器能力

- libmpv 进程内播放，不依赖外部 `mpv` 命令。
- 128K / 320K / FLAC / Hi-Res 等音质档位由音源实际提供情况决定。
- ReplayGain、均衡器、声道、左右平衡。
- 列表循环、单曲循环、随机、顺序、播完停止。
- A-B 循环、淡入、淡出、无缝切换。
- 在线播放失败时支持跨音源/解析器回退。

## 歌词与下载

支持 LRC、KRC、QRC、YRC 以及翻译/罗马音数据。下载歌词会过滤非标准 LRC 数据，避免把网易云接口原始 JSON 写入 `.lrc`。

下载支持并发分片、CDN 回退、重试、大小/MD5 校验、标签、封面、标准 LRC、取消与清理。失败任务只保留在当前下载任务视图，不再重复写入持久下载历史。

## 本地音乐

支持 MP3、FLAC、M4A、OGG、OPUS、WAV、WMA、AAC、APE、AIFF 等常见格式及 CUE 分轨。支持增量扫描、标签、封面、内嵌/同名歌词以及重复、损坏、缺失文件诊断。

## 当前音源

| 音源 | 搜索 | 播放 | 歌词 | 封面 | 歌手/专辑 | 歌单 | 榜单 | 链接直解 | 登录 |
|---|---:|---:|---:|---:|---:|---:|---:|---:|---:|
| 酷我 `kw` | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | — |
| 酷狗 `kg` | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | 扫码/个人歌单 |
| QQ `tx` | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | 扫码/个人歌单 |
| 网易云 `wy` | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | 扫码/个人歌单 |
| 咪咕 `mg` | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | — |
| 哔哩哔哩 `bili` | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | 扫码 |
| 千千 `qianqian` | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | — | ✓ | — |
| JOOX `joox` | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | — | ✓ | — |
| 5sing `fivesing` | ✓ | ✓ | ✓ | ✓ | — | ✓ | — | ✓ | — |
| Jamendo `jamendo` | ✓ | ✓ | ✓ | ✓ | — | ✓ | — | ✓ | — |
| Apple Music `apple` | ✓ | ✓（试听） | ✓ | ✓ | — | ✓ | — | ✓ | — |
| 汽水 `soda` | ✓ | ✓ | ✓ | ✓ | — | ✓ | — | ✓ | — |
| 本地 `local` | — | ✓ | ✓ | ✓ | ✓ | — | — | — | — |

“✓”表示代码层已实现/声明对应能力；具体歌曲仍可能受到平台接口、地区、账号权限或版权限制影响。Apple Music 当前为试听片段，不应视为完整曲目下载源。

JS 音源可以动态加载多个社区脚本，具体能力由脚本声明，可参与搜索、播放 URL、歌词和封面解析。

## 通知与平台

- Linux：D-Bus 桌面通知和 MPRIS。
- Windows：`Shell_NotifyIconW` 通知区气泡。
  - 使用 voicefox 应用图标，并遵循系统的提示音与勿扰设置。
  - 发送新通知前撤下本应用上一条气泡；其他应用正在显示气泡时，新通知仍可能进入 Windows 气泡队列。
  - 通知区图标在最后一次通知 10 秒后移除。
  - 不提供专辑封面、操作按钮和通知中心历史。
- Windows SMTC / 硬件媒体键：尚未实现。

## 数据目录

| 平台 | 数据目录 |
|---|---|
| Linux | `$XDG_DATA_HOME/voicefox`，没有时使用系统数据目录 |
| macOS | `~/Library/Application Support/voicefox` |
| Windows | `%LOCALAPPDATA%\voicefox` |

配置与会话仍使用平台配置目录。

## 0.3.15 修复重点

- 队列 `↑/↓` 选择冲突、鼠标命中与过滤索引。
- 队列筛选与全屏歌词。
- 长歌词自动换行。
- 多歌手分别查看。
- 下载上下选择、右键操作与帮助提示。
- 失败下载不再与历史重复展示。
- Windows 桌面通知。
- 平台数据目录规范。
- 网易云歌词落盘标准化。
- JS 音源非法 URL 不再触发 HTTP request builder panic。

## 发布验证

- `cargo fmt --all`
- `cargo test -p voicefox-app`
- `cargo test -p lx-core`
- `cargo test -p lx-lyric`
- `cargo check -p voicefox-app --target x86_64-pc-windows-gnu`
